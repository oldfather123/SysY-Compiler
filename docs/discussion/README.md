# 常见问题
## 1. 比赛内容

使用c或c++实现一个编译器，将SysY2020语言的代码翻译为Arm汇编代码

## 2. 测评流程

参赛队提交的编译器代码将首先在X86 CentOS的服务器上被编译运行，将测试样例的SysY2020语言的代码翻译为
Arm汇编代码，上述操作在X86服务器中进行。 之后再将生成的Arm汇编代码传入树莓派中进行汇编、链接并运
行，并依据运行结果进行评测。

## 3. 比赛技术平台网址

https://compiler.educg.net

## 4. 上界编译大赛资料参考

https://gitlab.eduxiji.net/nscscc/compiler2021/-/tree/master

## 5. 树莓派赛后是否收回？
   树莓派由华为赞助，赛后不收回。

## 6. 学生证丢了，是否可以用身份证替代？
   身份证不能证明本科生身份，可以用一卡通，或者学院出个证明即可。主要是能够证明本人来自哪所高校、什么时候入学。

## 7. _sysy_stoptime函数并没有在函数原型中对参数做出要求，但是为什么它的实现里读取了寄存器r0的值呢？
   因为在优化的时候把unused-value 删去了，导致stoptime的输出不正确，但是stoptime本身并没有说明要读取这个值呀

文档中提供的库函数声明混淆了“函数”和“宏”的概念，并非stoptime真实的声明。你可以从SysY运行时库的源代码（https://gitlab.eduxiji.net/nscscc/compiler2021/-/blob/master/%E5%85%AC%E5%BC%80%E7%94%A8%E4%BE%8B%E4%B8%8E%E8%BF%90%E8%A1%8C%E6%97%B6%E5%BA%93/sylib.h）中看到，所谓的starttime和stoptime只是两个宏，它们应该被展开成对_sysy_starttime和_sysy_stoptime的函数调用。而这两个函数的声明表示，它们都需要接收一个参数，也就是调用处的行号。所以实际上_sysy_stoptime的原型是要求函数参数的，只不过文档里对stoptime的描述把这个事情掩盖过去了。又因为ARM ABI的规定，第一个参数使用r0传递，所以最终r0的值被读取了。

## 8. 想问问sysy短路可以出现在if/while外边吗，比如这种：int a = f()&&g();
   不能

## 9. 这个运行时库样例里a[]的数组定义写法合法吗？
文档用C语言写了个伪代码来示意可以这么调用这个函数。根据SysY的语法是不允许这么写的。测试用例里，全没用到这个函数。