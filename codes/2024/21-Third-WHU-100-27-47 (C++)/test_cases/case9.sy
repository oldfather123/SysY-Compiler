int foo(int x, int a, int b, int c) {
    int d = a + b;
    int e = d + b;
    int f = a + b;
    int ret = f + d + a + b;
    while (x) {
        c = c + 1;
    }
    return d+d+d+d+e+f+c+ret;
}
