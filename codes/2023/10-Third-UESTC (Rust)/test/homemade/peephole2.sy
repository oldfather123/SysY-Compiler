int f() {
    int a = 2;
    int b = 4;
    return b / 2 + 2 * a - 4;
}
int main() {
    int b = f();
    int c = b / 2;

    if (c > 3)  {
        return b - 2;
    }

    return b + 4;
}