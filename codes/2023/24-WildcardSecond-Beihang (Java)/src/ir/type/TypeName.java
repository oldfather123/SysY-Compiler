package ir.type;

public enum TypeName {
    I, V, A, F,                 //integer, void, array, float
    IP,                         //integer pointer 保留，目前看没有作用
    FP,                         //Float pointer   保留，目前看没有作用
    P,                          //pointer
    FUNCTION, //function
    Vari
}
