// 公共子表达式消除测试

int main() {
    int a = 2;
    int b = 3;
    
    // 这两个表达式应该被合并
    int x = a + b;
    int y = a + b;  // 公共子表达式
    
    // 嵌套表达式测试
    int z = (a + b) * 2;  // 可以重用 a + b 的结果
    
    return x + y + z;
}