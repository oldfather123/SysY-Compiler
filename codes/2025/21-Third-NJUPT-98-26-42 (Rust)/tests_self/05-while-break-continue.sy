// 测试SSA构造的简单例子
int main() {
    int x = 1;
    while (x > 0) {
        x = x + 1;
        if (x > 10) {
            x = 0; // 退出循环
            break;
        } else {
            x = x * 2; // 继续循环
        }
    } 
    return x;
}