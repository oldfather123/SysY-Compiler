int g_arr[2][2];

int main() {
    int i, j;
    i = getint();  // This should prevent optimization
    j = getint();
    if (i < 0 || i >= 2) i = 0;
    if (j < 0 || j >= 2) j = 1;
    g_arr[i][j] = 5;
    return g_arr[i][j];
}