// 纯粹的使用测试 - else分支只使用变量，不重新定义
int test(int x) {
    if (x > 0) {
        // then分支什么都不做
    } else {
        return x + 1;  // 只使用x，不重新定义
    }
    return 0;
}

int main() {
    return test(5);
}