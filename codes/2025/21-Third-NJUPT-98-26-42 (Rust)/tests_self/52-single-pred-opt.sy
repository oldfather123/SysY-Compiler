// 测试单一前驱的优化
int main() {
    int x = 5;
    
    if (x > 0) {
        // 这个块只有一个前驱
        // 如果x没有被重新定义，块参数应该被优化掉
        int y = x + 1;  // 使用x但不重新定义它
        return y;
    }
    
    return 0;
}