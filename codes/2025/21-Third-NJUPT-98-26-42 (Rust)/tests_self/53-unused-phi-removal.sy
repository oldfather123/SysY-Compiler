// 测试未使用的块参数优化
int main() {
    int x = 1;
    int y = 2;
    
    if (x > 0) {
        y = 10;
    } else {
        y = 20;
    }
    
    // 这里不使用y，直接返回常量
    // 块参数应该被优化掉
    return 42;
}