int main() {
    int x = 0;
    while (x < 10) {
        if (x == 5) {
            break;
        } else {
            break;
        }
        x = x + 1; // unreachable
    }
    return x;
}