// 测试函数调用中的变量别名问题 - 别名情况
int swap(int a, int b) {
    return a + b;
}

int main() {
    int x = 1;
    int y = 2;
    
    // 别名情况：返回值赋给其中一个参数变量
    x = swap(x, y);  // 这里x既是参数又是返回值的接收者
    return x;
}