[package]
name = "simple-compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
cranelift-isle = { path = "../../crates/isle/isle", features = ["fancy-errors", "logging"] }
env_logger = "0.10"
log = "0.4"

[build-dependencies]
cranelift-isle = { path = "../../crates/isle/isle", features = ["fancy-errors", "logging"] }