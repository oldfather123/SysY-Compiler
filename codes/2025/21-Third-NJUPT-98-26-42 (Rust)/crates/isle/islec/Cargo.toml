[package]
name = "islec"
version = "0.0.0"
authors = ["The Cranelift Project Developers"]
edition = "2021"
rust-version = "1.76"
license = "Apache-2.0 WITH LLVM-exception"
publish = false

[dependencies]
cranelift-isle = { workspace = true, features = ["fancy-errors", "logging"] }
env_logger = { workspace = true }
clap = { workspace = true, features = ['default'] }
